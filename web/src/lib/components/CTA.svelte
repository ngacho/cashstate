<section class="cta" id="get-started">
	<div class="cta-inner">
		<h2>Ready to know the state<br/>of your cash?</h2>
		<p>CashState is completely free. Connect your SimpleFIN account and take control of your finances today.</p>
		<a href="#get-started" class="btn-primary">
			Get Started for Free
			<svg width="18" height="18" viewBox="0 0 16 16" fill="none">
				<path d="M3 8h10M9 4l4 4-4 4" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
			</svg>
		</a>
	</div>
</section>

<style>
	.cta {
		padding: 6rem 2rem;
		background: var(--color-dark);
		text-align: center;
	}

	.cta-inner {
		max-width: 600px;
		margin: 0 auto;
	}

	h2 {
		font-size: clamp(1.8rem, 4vw, 2.5rem);
		font-weight: 800;
		color: var(--color-white);
		line-height: 1.2;
		margin-bottom: 1rem;
	}

	p {
		font-size: 1.1rem;
		color: var(--color-text-light);
		margin-bottom: 2rem;
	}

	.btn-primary {
		display: inline-flex;
		align-items: center;
		gap: 0.5rem;
		padding: 0.9rem 2rem;
		background: var(--color-primary);
		color: white;
		border-radius: 100px;
		font-size: 1rem;
		font-weight: 600;
		transition: all 0.2s;
		box-shadow: 0 4px 14px rgba(13, 148, 136, 0.35);
	}

	.btn-primary:hover {
		background: var(--color-primary-dark);
		transform: translateY(-1px);
		box-shadow: 0 6px 20px rgba(13, 148, 136, 0.4);
	}
</style>
