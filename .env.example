# CashState — Convex Server Environment Variables
# Copy to .env and fill in:  cp .env.example .env
# Then push to Convex:       ./scripts/push-convex-env.sh

# Clerk JWT issuer URL (Clerk Dashboard > API Keys)
CLERK_ISSUER_URL=https://YOUR-APP.clerk.accounts.dev

# SimpleFin base URL
SIMPLEFIN_BASE_URL=https://beta-bridge.simplefin.org

# OpenRouter (AI categorization) — https://openrouter.ai/keys
OPENROUTER_API_KEY=
OPENROUTER_MODEL=meta-llama/llama-3.1-8b-instruct:free

# Encryption key for SimpleFin access URLs
# Generate one: openssl rand -base64 32
ENCRYPTION_KEY=
